# Saving data to MongoDB
- is a document database
- is low in complexity
- different in how they oganize data as well as the query language they use
- are a part of the NoSQL umbrella team


### Model
- are constructor functions that create new JavaScript oobjects based on provided parameters
- objects created with the model's constructor function have all the properties of the model -> in addition to methods for saving objects to the database (using the 'save' method, provided with event handler 'then')